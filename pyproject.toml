[project]
name = "testgram"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "aiohttp>=3.11.13",
    "pyyaml>=6.0.2",
]

[dependency-groups]
dev = [
    "ruff>=0.12.0",
]

[tool.ruff]
line-length = 88
